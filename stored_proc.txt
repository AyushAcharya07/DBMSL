--CREATE TABLE STUD_MARKS(Roll_no number(10) PRIMARY KEY,Student_Name varchar2(20),Total_Marks number(5));
--CREATE TABLE RESULT(Roll_no number(10),Student_Name varchar2(20),Class varchar(10),FOREIGN KEY(Roll_no) REFERENCES STUD_MARKS(Roll_no));

/*INSERT INTO STUD_MARKS VALUES (20,'DHRUV PANDIT',700);
INSERT INTO STUD_MARKS VALUES (26,'KRUTI JOSHI',1507);
INSERT INTO STUD_MARKS VALUES (35,'AANSH SHARMA',856);
INSERT INTO STUD_MARKS VALUES (37,'AYUSH ACHARYA',987);*/

SELECT * FROM STUD_MARKS;

DECLARE
	temp_marks number(5);
	temp_name varchar2(20);
	temp_roll number(5);

BEGIN
	CREATE PROCEDURE proc_GRADE @marks number(5),@name varchar2(20),roll number(5)
	AS
	BEGIN
    	SELECT Total_Marks,Student_Name,Roll_no
    	INTO temp_marks,temp_name,temp_roll
    	FROM STUD_MARKS
		WHERE STUD.Roll_no=roll;

		IF (temp_marks<=1500 AND temp_marks>=990) THEN
			DBMS_OUTPUT.PUT_LINE(name || 'is granted DISTINCTION ');

		ELSIF (temp_marks>=900 AND temp_marks<=989) THEN
			DBMS_OUTPUT.PUT_LINE(name || 'is granted FIRST CLASS');

		ELSIF (temp_marks>=825 AND temp_marks<=899) THEN
			DBMS_OUTPUT.PUT_LINE(name || 'is granted HIGHER SECOND CLASS');

		ELSE
			DBMS_OUTPUT.PUT_LINE(name || 'has FAILED ');

	END

	EXEC proc_GRADE @marks=700,@name='DHRUV PANDIT',@roll=20

END;

